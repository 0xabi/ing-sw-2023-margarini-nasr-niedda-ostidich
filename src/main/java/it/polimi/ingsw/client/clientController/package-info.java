/**
 * Client controller is to talk with the view when server communication is not set yet.
 * It asks for information on playing preferences and for connecting to server.
 * After this process it gives the server complete power over the view, until game ends.
 * It shows methods doable on the view so the server is able to call them.
 */
package it.polimi.ingsw.client.clientController;