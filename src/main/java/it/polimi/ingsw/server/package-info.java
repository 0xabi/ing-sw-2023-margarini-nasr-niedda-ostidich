/**
 * Server package wants to group all the sub-packages that constructs the server.
 * Server package can live by its own if it has resources folder in the same directory.
 */
package it.polimi.ingsw.server;